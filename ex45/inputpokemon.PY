next_entry = "Y"
target = open("C:\Users\Gerard\Desktop\pokemon1.txt", "w")

while next_entry.upper() == "Y":

	pokemon_name = raw_input("Pokemon Name: ")
	type1 = raw_input("Type 1: ")
	type2 = raw_input("Type 2: ")
	min_hp = raw_input("Minimum HP: ")
	max_hp = raw_input("Maximum HP: ")
	min_attack = raw_input("Minimum Attack: ")
	max_attack = raw_input("Maximum Attack: ")
	min_defense = raw_input("Minimum Defense: ")
	max_defense = raw_input("Maximum Defense: ")
	min_special = raw_input("Minimum Special: ")
	max_special = raw_input("Maximum Special: ")
	min_speed = raw_input("Minimum Speed: ")
	max_speed = raw_input("Maximum Speed: ")

	target.write("\t\"")
	target.write(pokemon_name)
	target.write("\": {\n")
	target.write("\t\t\t\t\"TYPE\": {\n")
	target.write("\t\t\t\t\t\"TYPE1\": \"")
	target.write(type1)
	target.write("\",\n")
	target.write("\t\t\t\t\t\"TYPE2\": \"")
	target.write(type2)
	target.write("\"},\n")
	target.write("\t\t\t\t\"BASE STATS\": {\n")

	target.write("\t\t\t\t\t\"BASE HP\": {\n")
	target.write("\t\t\t\t\t\t\"MIN HP\": ")
	target.write(min_hp)
	target.write(",\n")
	target.write("\t\t\t\t\t\t\"MAX HP\": ")
	target.write(max_hp)
	target.write("},\n")

	target.write("\t\t\t\t\t\"BASE ATTACK\": {\n")
	target.write("\t\t\t\t\t\t\"MIN ATTACK\": ")
	target.write(min_attack)
	target.write(",\n")
	target.write("\t\t\t\t\t\t\"MAX ATTACK\": ")
	target.write(max_attack)
	target.write("},\n")

	target.write("\t\t\t\t\t\"BASE DEFENSE\": {\n")
	target.write("\t\t\t\t\t\t\"MIN DEFENSE\": ")
	target.write(min_defense)
	target.write(",\n")
	target.write("\t\t\t\t\t\t\"MAX DEFENSE\": ")
	target.write(max_defense)
	target.write("},\n")

	target.write("\t\t\t\t\t\"BASE SPECIAL\": {\n")
	target.write("\t\t\t\t\t\t\"MIN SPECIAL\": ")
	target.write(min_special)
	target.write(",\n")
	target.write("\t\t\t\t\t\t\"MAX SPECIAL\": ")
	target.write(max_special)
	target.write("},\n")

	target.write("\t\t\t\t\t\"BASE SPEED\": {\n")
	target.write("\t\t\t\t\t\t\"MIN SPEED\": ")
	target.write(min_speed)
	target.write(",\n")
	target.write("\t\t\t\t\t\t\"MAX SPEED\": ")
	target.write(max_speed)
	target.write("}}},\n")

	next_entry = raw_input("Continue? ")

target.close()